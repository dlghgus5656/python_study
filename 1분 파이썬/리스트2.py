# 리스트는
리스트 = [값1, 값2, ...]
my_list = ['오예스', '몽쉘', '초코파이']
# - 인덱스, 슬라이싱 가능
# - in (어떤 값이 포함되어있는지 확인) 사용가능
# - 리스트 길이 len() 확인가능
# - 인덱스를 지정해 값 수정 가능 a_list[1] = '몽쉘카카오'
# - append(..) 맨뒤에 값 추가
# - remove(..) 해당 값 삭제
# - 다른 리스트를 합치려면 .extend(..) 리스트 확장

# insert() - 원하는 위치에 값 추가

# pop() - 원하는 위치(또는 마지막)의 값 삭제

# clear() - 모든 값 삭제

# sort() - 값 순서대로 정렬

# reverse() - 순서 뒤집기

# copy() - 리스트 복사

# count() - 어떤 값이 몇 개 있는지

# index() - 어떤 값이 어디에 있는지
